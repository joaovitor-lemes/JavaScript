const alunos = ['Luiz', 'Maria', ['tiago', 'Jonas']];



alunos.push('Joao'); // adiciona no final
alunos.unshift('Pedro'); // adiciona no começo
//alunos.pop(); // remove o ultimo

console.log(alunos);
console.log(alunos.slice(-1,));
